/** @type {import('tailwindcss').Config} */
module.exports = {
  darkMode: 'class', // Allow dark mode via `.dark` class rather than OS only
  content: ["./src/**/*.{ts,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
}; 